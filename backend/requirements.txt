# FastAPI core
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.5.2

# Database & Auth
supabase==2.9.1

# AI / LLM
# Groq SDK — used for Whisper audio transcription (Gemini handles classification + RAG)
groq>=0.15.0
# Google Gemini API — used for classification, RAG, and embeddings (text-embedding-004)
google-genai

# Background jobs
celery==5.4.0
redis==5.1.1

# Environment & utilities
python-dotenv==1.0.1
httpx>=0.27.2

# Instagram reel scraping (services/downloader.py)
# curl_cffi: TLS fingerprint impersonation so Instagram serves us the real HTML
# parsel: locate the embedded application/json <script> blob
curl_cffi==0.7.4
parsel==1.9.1

# Web search fallback
tavily-python==0.5.0

# Push notifications
firebase-admin==6.5.0

# FFmpeg (bundled binary — no system install needed; also detects system ffmpeg as fallback)
imageio-ffmpeg>=0.5.1

# Testing
pytest>=8.0.0
pytest-mock>=3.14.0
